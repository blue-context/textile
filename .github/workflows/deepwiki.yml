name: DeepWiki Documentation

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  update-wiki:
    name: Update DeepWiki
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Update DeepWiki
      uses: erayack/deepwiki-action@v1
      with:
        repository: blue-context/textile
